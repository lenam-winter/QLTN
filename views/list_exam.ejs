<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Danh sách đề thi</title>
  <style>
    body { background: #764ba2; color: #fff; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
    .container { max-width: 800px; margin: 40px auto; background: rgba(255,255,255,0.13); padding: 32px 24px; border-radius: 12px; }
    h2 { margin-bottom: 18px; }
    .exam-block { margin-bottom: 24px; padding-bottom: 12px; border-bottom: 1px solid #fff3; }
    .exam-block:last-child { border-bottom: none; }
    a { color: #ffda77; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Danh sách đề thi</h2>
    <% exams.forEach(exam => { %>
      <div class="exam-block">
        <strong>Tên đề thi:</strong> <%= exam.name %><br>
        <strong>Thời gian:</strong> <%= exam.duration %> phút<br>
        <strong>Số câu hỏi:</strong> <%= exam.questions.length %>
      </div>
    <% }) %>
    <a href="/exams/add">Tạo đề thi mới</a>
    <br>
    <a href="/admin/dashboard" style="display:inline-block; margin-top:16px;">⬅ Quay lại trang quản trị</a>
  </div>
</body>
</html>