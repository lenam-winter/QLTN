<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <title>Danh sách lớp</title>
  <style>
    body {
      background: #f8f9fa;
      color: #2c3e50;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      margin: 0;
      padding: 20px;
    }
    .container {
      max-width: 800px;
      margin: 40px auto;
      background: #ffffff;
      padding: 32px;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    }
    h2 {
      margin-bottom: 24px;
      color: #2c3e50;
      font-size: 1.8rem;
      text-align: center;
      font-weight: 600;
    }
    .form-container {
      background: #f8f9fa;
      padding: 24px;
      border-radius: 12px;
      margin-bottom: 32px;
      border: 1px solid #e9ecef;
    }
    label {
      display: block;
      margin-bottom: 12px;
      font-weight: 600;
      font-size: 1.1rem;
      color: #495057;
    }
    input[type="text"] {
      width: 100%;
      padding: 12px;
      margin-bottom: 16px;
      border-radius: 8px;
      border: 2px solid #e9ecef;
      background: #ffffff;
      color: #2c3e50;
      font-size: 1rem;
      transition: all 0.3s ease;
    }
    input[type="text"]:focus {
      outline: none;
      border-color: #4dabf7;
      box-shadow: 0 0 0 3px rgba(77, 171, 247, 0.1);
    }
    input[type="text"]::placeholder {
      color: #adb5bd;
    }
    button {
      padding: 12px 28px;
      background: #4dabf7;
      color: #ffffff;
      border: none;
      border-radius: 8px;
      font-weight: 600;
      font-size: 1rem;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 2px 8px rgba(77, 171, 247, 0.2);
    }
    button:hover {
      background: #339af0;
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(77, 171, 247, 0.3);
    }
    .class-block {
      background: #ffffff;
      margin-bottom: 16px;
      padding: 16px 20px;
      border-radius: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: all 0.3s ease;
      border: 1px solid #e9ecef;
    }
    .class-block:hover {
      background: #f8f9fa;
      transform: translateX(5px);
      border-color: #4dabf7;
    }
    .class-block strong {
      font-size: 1.1rem;
      color: #4dabf7;
    }
    .delete-btn {
      background: #ff6b6b;
      color: #ffffff;
      padding: 8px 16px;
      border-radius: 6px;
      font-size: 0.9rem;
      transition: all 0.3s ease;
      border: none;
      box-shadow: 0 2px 6px rgba(255, 107, 107, 0.2);
    }
    .delete-btn:hover {
      background: #fa5252;
      transform: translateY(-1px);
      box-shadow: 0 4px 8px rgba(255, 107, 107, 0.3);
    }
    .back-link {
      color: #4dabf7;
      display: inline-block;
      margin-top: 24px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s ease;
      padding: 8px 16px;
      border-radius: 6px;
      background: #f8f9fa;
      border: 1px solid #e9ecef;
    }
    .back-link:hover {
      background: #e9ecef;
      transform: translateX(-5px);
      color: #339af0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="form-container">
      <h2>Tạo lớp mới</h2>
      <form method="POST" action="/classes/create">
        <label for="className">Tên lớp:</label>
        <input type="text" id="className" name="className" required placeholder="Nhập tên lớp..." />
        <button type="submit">Tạo lớp</button>
      </form>
    </div>
    <h2>Danh sách lớp</h2>
    <% classes.forEach(cls => { %>
      <div class="class-block">
        <strong>Lớp:</strong> <%= cls.className %>
        <form method="POST" action="/classes/delete/<%= cls._id %>" style="display:inline;">
          <button type="submit" class="delete-btn" onclick="return confirm('Bạn có chắc muốn xoá lớp này?')">
            Xoá
          </button>
        </form>
      </div>
    <% }) %>
    <a href="/admin/dashboard" class="back-link">⬅ Quay lại trang quản trị</a>
  </div>
</body>
</html>